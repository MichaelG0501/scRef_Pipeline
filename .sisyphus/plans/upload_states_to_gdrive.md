# Plan: Upload 'states' / 'comparison' PDFs to gdrive:IMPERIAL/

## Purpose
Copy (never move) a curated set of PDFs under `ref_outs/` to the existing rclone remote `gdrive:IMPERIAL/`.
The user explicitly approved this action. Files MUST NOT be modified or deleted locally.

## Files to copy
- ref_outs/Auto_states_studybias.pdf
- ref_outs/Auto_cluster_states_umap.pdf
- ref_outs/Auto_states_confusion.pdf
- ref_outs/Auto_states_coherence_cluster.pdf
- ref_outs/Auto_cluster_states_singlecell_heatmap.pdf
- ref_outs/Auto_cluster_states_proportion.pdf
- ref_outs/Auto_topmp_states_violin.pdf
- ref_outs/Auto_topmp_states_proportion.pdf
- ref_outs/Auto_states_comparison_umap.pdf
- ref_outs/Auto_topmp_states_singlecell_heatmap.pdf
- ref_outs/Auto_cluster_states_composition.pdf
- ref_outs/Auto_cluster_states_mean_heatmap.pdf
- ref_outs/Auto_cluster_states_silhouette.pdf
- ref_outs/Auto_topmp_states_mean_heatmap.pdf

## Safety checklist (MUST DO)
1. Run `module load rclone` before any rclone command on the login node.
2. Verify the `gdrive` remote exists: `rclone listremotes`.
3. Verify the `IMPERIAL/` remote folder is accessible: `rclone lsd gdrive:IMPERIAL/`.
4. Verify each local file exists and is readable: `test -r <path>`.
5. Use `rclone copy` (never `move`) without `--delete` or `--modify`.
6. Report per-file success/failure and list remote presence after copying.

## Exact commands (run in repository root)
Workdir: `/rds/general/ephemeral/project/tumourheterogeneity1/ephemeral/scRef_Pipeline`

1) Load rclone and verify remote

```
module load rclone
rclone listremotes
rclone lsd gdrive:IMPERIAL/ || echo "Warning: gdrive:IMPERIAL may not exist or be accessible"
```

2) Verify local files

```
files=(
  "ref_outs/Auto_states_studybias.pdf"
  "ref_outs/Auto_cluster_states_umap.pdf"
  "ref_outs/Auto_states_confusion.pdf"
  "ref_outs/Auto_states_coherence_cluster.pdf"
  "ref_outs/Auto_cluster_states_singlecell_heatmap.pdf"
  "ref_outs/Auto_cluster_states_proportion.pdf"
  "ref_outs/Auto_topmp_states_violin.pdf"
  "ref_outs/Auto_topmp_states_proportion.pdf"
  "ref_outs/Auto_states_comparison_umap.pdf"
  "ref_outs/Auto_topmp_states_singlecell_heatmap.pdf"
  "ref_outs/Auto_cluster_states_composition.pdf"
  "ref_outs/Auto_cluster_states_mean_heatmap.pdf"
  "ref_outs/Auto_cluster_states_silhouette.pdf"
  "ref_outs/Auto_topmp_states_mean_heatmap.pdf"
)

for f in "${files[@]}"; do
  if [ -r "$f" ]; then
    stat -c '%n\t%y\t%s' "$f"
  else
    echo "MISSING: $f"
  fi
done
```

3) Copy files (safe, per-file)

```
for f in "${files[@]}"; do
  echo "Copying: $f"
  rclone copy --progress "$f" gdrive:IMPERIAL/ && echo "COPIED: $f" || echo "FAILED: $f"
done
```

4) Verify remote presence (simple listing)

```
for f in "${files[@]}"; do
  bn=$(basename "$f")
  echo -n "$bn: "
  rclone lsf gdrive:IMPERIAL/ | grep -F "$bn" >/dev/null && echo "REMOTE_HAS" || echo "REMOTE_MISSING"
done
```

## MUST NOT DO
- Do not delete, move, or modify any local repo files.
- Do not push commits or change git state.
- Do not use --delete or --modify flags with rclone.

## After-action
- Present a per-file report (COPIED / FAILED / REMOTE_HAS / REMOTE_MISSING).

---
Generated by Sisyphus plan creation on user approval.
